import QtQuick
import qml

AppField {
    fieldFontFamily: Theme.medium
    inkColor: Theme.ink
    mutedInkColor: Theme.ink3
    primaryColor: Theme.primary
    dangerColor: Theme.red
    lineColor: Theme.line
}
